<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
             https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="bankPU">
        <!-- Здесь указываем все Entity-классы -->
        <class>org.example.logic.model.BankAccount</class>
        <!-- Добавь сюда Transaction, когда создашь -->
        <!-- <class>org.example.logic.model.Transaction</class> -->

        <properties>
            <!-- JDBC-подключение к H2 -->
            <property name="jakarta.persistence.jdbc.url" value="jdbc:h2:mem:testdb"/>
            <property name="jakarta.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>

            <!-- Генерация схемы -->
            <property name="jakarta.persistence.schema-generation.database.action" value="create"/>

            <!-- Лог SQL в консоль -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>